<div class="table-actions">

    <a
        href="{{ path('user_edit', {'id': user.id}) }}"
        class="table-action">

        Modifier

    </a>

    <a
        href="{{ path('user_delete', {'id': user.id}) }}"
        class="table-action table-action-danger delete-user"
        data-token="{{ csrf_token('delete-user-' ~ user.id) }}">

            Supprimer

    </a>

</div>
