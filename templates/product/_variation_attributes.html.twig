<div class="variation-attributes">
    {% for nom, valeur in variation.attributs %}
        <span class="variation-attribute">
            <strong>{{ nom }} :</strong> {{ valeur }}
        </span>
    {% else %}
        <span class="variation-standard">Standard</span>
    {% endfor %}
</div>
